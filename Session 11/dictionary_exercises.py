def is_in_dict():
    word = {}
    fin = open('words.txt')
    for line in fin:
        word = word + line
    

print(is_in_dict())

def has_duplicates(list):
    for word in x: 
        if word in x > 1:
            return True
        else: 
            return False
        
has_duplicates(["cat, cat"])
